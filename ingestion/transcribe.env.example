# Copy to ingestion/transcribe.env and customize per machine.
# All scripts auto-load this file when present.

# Where the original course videos are mounted.
TRANSCRIBE_SOURCE_BASE="/mnt/10PinesCourses"

# Where transcripts/logs/state are written.
TRANSCRIBE_OUT_BASE="$HOME/code/dojo/ingestion/raw"
TRANSCRIBE_STATE_DIR="$HOME/code/dojo/ingestion/raw"

# Optional runtime knobs
# TRANSCRIBE_CHUNK_SECONDS=600
# TRANSCRIBE_PYTHON="$HOME/code/dojo/.venv/bin/python"
# TRANSCRIBE_COURSES="diseno-a-la-gorra,heuristicas-diseno-software"
